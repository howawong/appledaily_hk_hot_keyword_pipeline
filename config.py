from os.path import join as join_path
LINKS_CSV = join_path("output", "links.csv")
LIKE_COUNTS_CSV = join_path("output", "like_counts.csv")
KEYWORDS_SCORE_CSV = join_path("output", "keywords_scores.csv")
KEYWORDS_URLS_CSV = join_path("output", "keywords_urls.csv")
CONTENTS_JSON = join_path("output", "contents.json")
ACCESS_TOKEN = ''
